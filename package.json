{
  "name": "ai_bot",
  "private": true,
  "description": "Repository root tooling (docs validation, etc.). App code lives in backend/ and frontend/.",
  "scripts": {
    "check:presentation-toc": "node scripts/check-presentation-toc.mjs"
  },
  "devDependencies": {
    "github-slugger": "^2.0.0"
  }
}
